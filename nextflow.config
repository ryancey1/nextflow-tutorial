profiles {
    
    conda { 
        process.conda = "$projectDir/env.yml"
    }

    docker {
        process.container = 'nextflow/rnaseq-nf'
        docker.enabled = true   // exercise 2.1
        docker.runOptions='-u $(id -u):$(id -g)'
    }

}